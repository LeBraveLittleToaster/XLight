package de.pschiessle.xlight.xlightserver.validator;

import de.pschiessle.xlight.xlightserver.components.MtsManipulator;
import de.pschiessle.xlight.xlightserver.components.MtsMood;
import java.util.List;
import reactor.core.publisher.Mono;

public class MtsMoodValidator {
  /*
  public static Mono<MtsMood> validateInsertMtsMood(String name, List<MtsManipulator> manipulatorList){
    if(name == null
        || name.length() == 0
        || manipulatorList == null
        || manipulatorList.size() == 0
    || manipulatorList.stream().filter(m -> MtsManipulatorValidator)){

    }
  }
  */
}
